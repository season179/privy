VERDICT: REJECT

Scope verified clean before listing findings (no findings in these areas): exact hourly split with sample conservation (`ShadowChunkWriter.append` loop + in-code assert, tested at rotationSamples=640 with 1000-sample crossing block, persisted durations 640/360 sum to 1000); EOS-less durable close flushes pages, syncs, closes, injects no packet and leaves granule unchanged (`OggOpusWriter.durablyCloseWithoutEndOfStream`); write→synchronize→rename→DB ordering in `finalizeCurrent`; OpusHead pre-skip conversion (`lookahead * 48000 / sampleRate`) is correct — verified against libopus 1.6.1 source (`OPUS_GET_LOOKAHEAD` returns `Fs/400 + delay_compensation` in input-rate samples, RFC 7845 pre-skip is 48 kHz units); exact 4096-sample windows with leftover carry and unchanged state pass-through (tested); FluidAudio 0.15.5 API usage matches the checked-out package (`VadManager` actor, `processStreamingChunk(_:state:config:returnSeconds:timeResolution:)`, `VadStreamResult{state,event,probability}`, `VadSegmentationConfig.default`, cache dir `~/Library/Application Support/FluidAudio/Models`); boundaries mapped via source sample/monotonic positions incl. across rotation (tested); lane overflow bumps generation → `vad.reset` + `vadGap` and never blocks the writer (tested); model init via `Task.detached` off main actor; GapWatchdog arm/disarm/2 s/5 s/10 s-grace rules match the contract; both mandatory named pipeline tests exist, are meaningful, and pass. `swift build --target PrivyCore` and all three permitted test filters pass (9 + 5 + 7 tests). No contract files modified; no `@unchecked Sendable` added in W4 code; `OggOpusWriter` stays thread-unsafe inside the writer actor.

1. **MAJOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:508-512` (`receive(_ event:)`, `.engineStopped(.fatalError)`): the pipeline sets `.error` but never calls `writer.close(reason: .fatalError)`. The plan's mandatory terminal-state mapping table (plan.md "Writer and VAD": `.fatalError` → stop accepting blocks, preserve file, run same-process writer recovery, transition to `ready`/`failed`) is implemented in `ShadowChunkWriter.close` and unit-tested, but is unreachable in production — the only caller never invokes it. A fatal engine stop leaves a `recording` row and an open `.partial` writer for the remaining process lifetime, violating crash/lifecycle invariant 6 ("exactly one row is `recording`… invokes the mandatory terminal-state recovery table"). Same omission on the other two fatal capture paths: `.inputUnavailable` (line 518-522) and the watchdog `heartbeatFault` 5-second path (line 744-760) declare capture dead while the writer's chunk stays open and non-terminal until `shutdown()` or process death. Fix: on `.engineStopped(.fatalError)`, `.inputUnavailable`, and `heartbeatFault`, call `writer.close(reason: .fatalError, at:)` (mapping to the same-process recovery table) and persist the resulting `writerError`/`recovery` health evidence.

2. **MAJOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:663-674` (`flushVADEvents`): a single failed `store.appendVADEvents` sets `captureReality = .error("VAD event persistence failed")`, records only a local (unpersisted) health row, and from then on `receive(_ block:)` (line 377) silently discards every audio block — recording is permanently halted by the VAD lane. This directly violates the frozen contract comment on `VADRuntimeStatus` ("`failed` reduces only VAD coverage; it must never stop or back-pressure recording") and the W4 scope rule ("A full VAD lane … never delays the writer"); a VAD-side persistence hiccup must not kill capture. The open chunk is left dangling until the watchdog happens to finalize it via the heartbeat-timeout path. Fix: on VAD-event persistence failure, drop/coalesce the batch, best-effort persist a `.vadError` health row, update `vadRuntimeStatus`, and leave `captureReality` untouched. Genuine store outages are still surfaced through the writer's checkpoint/finalize failure path, which has the sanctioned terminal-recovery behavior.

3. **MAJOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:377-378` and `390-397`: audio blocks arriving while in `.error` are silently discarded with no health event, no gap row, and no snapshot — and the writer-failure path (line 392-395) sets `engineRunning = false` without ever calling `capture.stop`, so the real engine keeps running and producing audio that vanishes at the pipeline with zero telemetry. This violates crash/lifecycle invariant 7 ("engine downtime … all produce structured health rows"; gap accounting must be complete) and the review priority on silent discard without health/snapshot evidence. Fix: stop the engine when entering a terminal `.error` (writer failure, heartbeat fault, input unavailable), and/or coalesce discarded-block telemetry into a structured `.gap` health row with accumulated duration.

4. **MAJOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:379-460` (`receive(_ block:)`): lifecycle state is checked once at entry and never re-validated after the `await writer.append(block)` suspension. Because the pipeline actor is reentrant, `pause()`/`willSleep()`/`shutdown()` can run their full intentional-stop bookkeeping while a receive is suspended inside the writer; when the receive resumes, `writer.activeChunk()` returns nil (the chunk was just finalized by the intentional close) and the else-branch at line 459-460 overwrites `.paused(untilUTC:)` / `.recovering("system sleep")` / `.stopped` with `.recovering("audio heartbeat is stale")`. Final snapshot after such a pause shows a false fault state for the entire pause — violating "Paused time is intentional and represented by `recordingStopped`, not treated as an unexplained fault" and "the menu icon is a pure rendering of `PipelineSnapshot.capture`". (The mandatory 1-hour pause test cannot catch this: it never asserts the post-pause snapshot is `.paused`.) Fix: re-check `shuttingDown`/`captureReality` immediately after the `append` await and bail out when an intentional-stop transition has interleaved (and add a test asserting the post-pause snapshot).

5. **MINOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:524-548` + `482-501`: one physical overrun gap is accounted twice. The `.queueOverrun` handler persists `.gap` + `.vadGap`; the audio contract guarantees "the next accepted block retains the timeline gap", so `continuityGap(before:)` fires on that next block and persists a second `.gap` + `.vadGap` (plus a second `vadGeneration` reset) for the same lost audio. Fix: reconcile the two paths — e.g., after an overrun event, advance/reset continuity tracking so the event-driven row is the single accounting of that gap.

6. **MINOR** — `Sources/OpusIO/OggOpusWriter.swift:90-104` with `Sources/PrivyCore/Recording/ShadowChunkWriter.swift:244-247`: `finish()` is not idempotent across a partial failure, despite the recovery comment claiming "Both close operations are idempotent". If `finish()` throws after `encodeFrame(EOS)` (failure in `flushPages`/`synchronize`/`close`, `finished` still false), the recovery retry takes the `else` branch and pads+encodes a *second* full-frame zero EOS packet, injecting up to 20 ms of spurious silence and a duplicate EOS into the recovered file. Fix: mark EOS-submitted (or set `finished`) before encoding and make the flush/sync/close tail separately retryable.

7. **MINOR** — `Sources/PrivyCore/Recording/ShadowChunkWriter.swift:339`: `probeDuration` calls `process.waitUntilExit()` synchronously on the writer actor, blocking a cooperative-pool thread for the lifetime of an external process during failure recovery (the path taken precisely when the system is already degraded). Fix: make the probe non-blocking (readability/termination handlers) or hop off the actor.

8. **MINOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:323` + `349-357`: `performDeviceRestart` nils `pendingDeviceUID` before calling `restartCapture`, which then persists the `.engineRestart` health row with `deviceUID: pendingDeviceUID` — always nil. Plan requires one structured `.deviceChange` plus `.engineRestart`; the restart row loses the device attribution. Fix: pass the captured `uid` through to `restartCapture`.

9. **MINOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:458-460`: the stale-heartbeat branch disarms the watchdog and enters `.recovering("audio heartbeat is stale")` with no escalation path — if the stream then dies, no 2 s/5 s threshold ever fires and no `.gap`/`.error` is persisted; the pipeline sits in `.recovering` indefinitely (watchdog contract: "A stalled/dead capture outside grace cannot publish `.recording` beyond the 2/5-second thresholds" — here it can stall *forever* without any terminal truth). Fix: keep the watchdog in a recovery mode with the original heartbeat as baseline, or emit a measured gap + error after the threshold.

10. **MINOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:471-477` (and the `.terminated` branch at 479-488): VAD lane drop telemetry sets `droppedFrames: Int64(block.samples.count)` — converted 16 kHz samples — while the same `HealthDetail.droppedFrames` field carries *source* frames everywhere else (`.queueOverrun` uses `droppedSourceFrames`). Fix: use consistent semantics (or omit the field for converted-sample drops).

11. **MINOR** — `Sources/PrivyCore/Recording/VADService.swift:88-90`: `process` throws `nonFiniteAudio` without flipping `runtimeStatus` to `.failed` (unlike model-processing errors at line 137-140). The pipeline catches each throw and persists a `.vadError` health row *per block forever* while snapshots keep reporting `vad == .ready` — untruthful status plus health spam. Fix: set `.failed` (or drop the non-finite block with a single coalesced health row).

12. **MINOR** — `Tests/PrivyTests/ShadowCapturePipelineTests.swift`: test-honesty gaps, not bypasses (the suites do drive the real writer/VAD seam/fake clock end-to-end): (a) no test asserts the post-`pause()` snapshot is `.paused`, which is exactly what finding 4 breaks; (b) no pipeline test exercises `.engineStopped(.fatalError)` or `heartbeatFault` → writer terminal-state wiring, which is why finding 1 shipped; (c) no test exercises a `store.appendVADEvents` failure, the path in finding 2. Fix: add the three assertions/tests above through the existing injected seams.

## Re-review

Delta reviewed: `df9b9c3..0f9c187` (0e1380c "Harden writer failure recovery", 0f9c187 "Fix pipeline terminal failure handling"). Re-verified locally: `swift build --target PrivyCore` clean; `ShadowChunkWriterTests` 9/9, `VADServiceTests` 6/6, `ShadowCapturePipelineTests` 12/12 pass. Constraint check: no files under `Sources/PrivyCore/Contracts/` touched; `OggOpusWriter` public method set unchanged (only private state renamed); `FFprobeRunner` is `private`; no protocol signatures widened.

Disposition of original findings:

1. **FIXED.** `.engineStopped(.fatalError)` (ShadowCapturePipeline.swift:556-562), `.inputUnavailable` (571-576), `heartbeatFault` (810-819), and `wakeGraceExpired` (821-830) all route through `enterTerminalCaptureError(recoverWriter: true)`, which disarms the watchdog, stops the engine with `.fatalError`, calls `writer.close(reason: .fatalError)`, and persists a `.recovery` row when an active chunk was terminally resolved plus one `.gap`/`.error` pair. `heartbeatFaultInvokesFatalWriterCloseWiring` (spy writer asserts `.restart` then `.fatalError` closes + fatal capture stop) and `fatalEngineStopRunsWriterTerminalRecoveryAndStopsCapture` (real writer; chunk reaches ready/failed with recovery evidence) prove the wiring.
2. **FIXED.** `flushVADEvents` (722-731) now drops the batch, latches `vadPersistenceFailed`, sets `vadRuntimeStatus = .failed`, resets VAD continuity, and persists `.vadError` best-effort without touching `captureReality`; the lane drains without further processing. `VADEventStoreFailureKeepsCaptureRecordingAndMarksOnlyVADFailed` asserts snapshot stays `.recording`, chunk stays `recording`, and no fatal stop occurs.
3. **FIXED.** All terminal-error entries call `capture.stop(reason: .fatalError)` (848-849); blocks arriving in `.error` go to `recordDiscardedErrorBlock`, which coalesces them into a single structured `.gap` ("discarded N converted samples after terminal capture failure") with source-position start/end and sample-derived duration, flushed after 250 ms quiescence, on terminal entry, and on shutdown. Covered by the tail of `fatalEngineStopRunsWriterTerminalRecoveryAndStopsCapture`.
4. **FIXED.** `lifecycleGeneration` is bumped on every intentional-stop path (pause/willSleep/device restart/watchdog restart/shutdown/terminal error); `receive` re-checks generation, `shuttingDown`, and `captureReality` after the `append` await, and routes stale completions through `closeWriterAfterStaleAppend`, which closes any chunk the interleaved append opened using the in-flight `intentionalStopReason` (so the mapping table is honored) and never overwrites the post-stop snapshot. `pauseInterleavingWithWriterAppendCannotOverwritePausedSnapshot` exercises the exact race deterministically with a suspending writer and asserts the final snapshot is `.paused` and no chunk is left open. The 1-hour pause test now also asserts the post-pause `.paused` snapshot.
5. **FIXED.** `accountedOverrunAwaitingNextBlock` suppresses the continuity-derived `.gap`/`.vadGap` for exactly the next block after a `.queueOverrun`; the updated overrun test emits the gap-carrying successor block and asserts exactly one `.gap` and one `.vadGap`.
6. **FIXED.** `OggOpusWriter.finish()` commits `pending.removeAll()` + `closePacketAttempted = true` before encoding the EOS frame; a retry after a post-encode failure re-runs only flush/sync/close and cannot inject a second padded EOS packet. `append`/`synchronize` preconditions now reject use after a close attempt instead of silently writing into a half-closed stream.
7. **FIXED.** `probeDuration` is now `async` via private `FFprobeRunner` (termination-handler continuation + cancellation-driven `terminate()`); no cooperative-pool thread is blocked. (Residual, pre-existing pattern, not flagged: reading pipes only after termination is safe here only because `ffprobe -v error -show_entries format=duration` output is far below pipe-buffer capacity.)
8. **FIXED.** `performDeviceRestart` passes the captured `uid` through `restartCapture(deviceUID:)`; the test asserts the `.engineRestart` row carries `deviceUID == "airpods"`.
9. **FIXED.** The stale-heartbeat branch now arms the watchdog with the block's heartbeat when disarmed (505-509), so a stream that dies after stale blocks still hits the 2 s restart / 5 s fault ladder; `firstStaleHeartbeatCannotRemainRecoveringForever` proves escalation. Behavioral note (accepted): one stale block now provokes a real engine restart attempt — consistent with the contract's "heartbeat older than 2 s is not recording reality".
10. **FIXED.** Lane-drop health no longer populates `droppedFrames`; messages state "converted block".
11. **FIXED.** `VADService.process` sets `.failed` and clears continuity on non-finite input; `nonFiniteAudioFailsRuntimeOnceAndPreventsRepeatedErrorSpam` asserts one throw, `.failed` status, and silent empty processing thereafter.
12. **FIXED.** See tests enumerated above (post-pause snapshot, fatal-stop wiring, VAD-store-failure).

New findings in the delta:

13. **MAJOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:42, 96, 201, 230, 843-844`: `terminalFailureInProgress` is a permanent latch. It is set in `enterTerminalCaptureError` and cleared only in `start()`, which is unreachable after a terminal failure (requires `.stopped`, i.e. shutdown). But recovery from `.error` is still possible: `performDeviceRestart` is not state-gated, so a device-change event rebuilds capture and the pipeline returns to `.recording` — with the latch still set. From then on (a) `pause()`/`resume()` are silently ignored forever (guards at 201/230), killing a user-facing `ShadowCaptureControlling` control for the rest of the process, and (b) any subsequent terminal failure is swallowed by the guard at 843: no state transition, no engine stop, no writer terminal recovery, no health row — if the writer is terminally blocked, every `append` throws, is discarded by the no-op `enterTerminalCaptureError`, and the snapshot keeps publishing `.recording` while nothing is written (watchdog restart and 5 s fault are swallowed the same way), a direct violation of "snapshot is observed reality" and invariant 6. Fix: clear `terminalFailureInProgress` when capture verifiably returns to `.recording` (or on successful `restartCapture`), so the latch scopes one failure episode instead of the process lifetime; add a test: terminal error → device-change recovery → pause works and a second injected writer failure produces full terminal handling.
14. **MINOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:410-413`: `accountedOverrunAwaitingNextBlock` suppresses the next block's continuity-derived gap unconditionally, even when that block's discontinuity is larger than or unrelated to the overrun the event described (e.g., overrun telemetry with no converted-timeline gap, followed by a genuine gap). In that corner a real gap loses its health row. Fix: compare the block's measured discontinuity against the overrun's accounted duration and log only the unaccounted remainder (or key the flag on the expected resumed `streamSampleStart`).

RE-VERDICT: REJECT

## Re-review round 3

Delta reviewed: `0f9c187..9e3f0a8` ("Scope recovery episodes and overrun accounting"; 2 files, +208/-9). Re-verified locally: `swift build --target PrivyCore` clean; `ShadowChunkWriterTests` 9/9, `VADServiceTests` 6/6, `ShadowCapturePipelineTests` 14/14 pass (including the two new tests below). No contract files touched; no API changes.

Disposition of round-2 findings:

13. **FIXED.** `terminalFailureInProgress` is now cleared at exactly the verifiable return to `.recording` — inside `receive`, only on the non-stale path (post generation/shuttingDown/state re-checks), only when a fresh heartbeat with an open chunk transitions `captureReality` into `.recording` (ShadowCapturePipeline.swift:515-519). Soundness: `enterTerminalCaptureError` always moves the state to `.error` and bumps `lifecycleGeneration`, and the only route back to `.recording` passes through this branch, so the latch provably scopes one episode. Race-freedom: stale in-flight receives return before the heartbeat section (generation bump at episode entry), receives starting mid-episode see `.error` and only record discard telemetry, and `performDeviceRestart` is deliberately not gated on the latch — the device-restart-while-in-error recovery story is coherent (watchdog stays disarmed until the first fresh heartbeat re-arms it, per the contract). `recoveredTerminalEpisodeAllowsPauseAndHandlesSecondWriterFailure` proves the full arc: fatal stop → `.error` → device-change recovery → `.recording` → pause/resume work → injected checkpoint failure → full terminal handling again (second fatal engine stop, `.writerError` health, zero stuck `recording` rows).
14. **FIXED.** Suppression is now keyed: `AccountedOverrun{captureEpoch, streamEndBeforeDrop, expectedResumedStreamStart}`, with the baseline taken from the in-flight block when an overrun event interleaves a suspended `append` (the `inFlightCaptureEpoch`/`inFlightStreamEnd` tracking is set before the await and cleared on every exit — and audio receives are strictly serial, so only the event task can observe this window). `reconciledContinuityGap` suppresses only when the next block matches the epoch and resumes at/beyond the accounted point, and remainder-logs any excess as an "unaccounted source timeline discontinuity" gap. `overrunSuppressesOnlyAccountedPortionOfLargerFollowingGap` proves it: 0.1 s overrun + a 0.2 s actual gap yields exactly two `.gap` rows summing to 0.2 s, one tagged "unaccounted". The fully-accounted case (existing device-burst test) still yields exactly one `.gap`.

Residual findings (both narrow corners, neither blocking):

15. **MINOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:598-608`: consecutive `.queueOverrun` events before the next block overwrite `accountedOverrun` instead of accumulating. Two overruns within one block interval log event gaps d1 and d2, then the remainder computation uses only d2 as accounted, emitting a third gap row of d1 — d1 is double-counted. Fix: when `accountedOverrun` already exists, add `normalizedDrop` to `expectedResumedStreamStart` rather than replacing the struct.
16. **MINOR** — `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:843-844` with `385-390` / `310-317`: the latch is only cleared on *successful* recovery. A terminal failure *during* the recovery window (e.g., device-change rebuild whose `capture.restart` throws, or a wake restart failing while a previous episode is latched) hits the `guard !terminalFailureInProgress` early-return and is fully swallowed: no health row, no state change, watchdog disarmed — the pipeline is left in `.recovering` indefinitely instead of `.error`. Fix: scope the guard to re-entrancy during the `enterTerminalCaptureError` body itself (e.g., clear it at the end of the terminal-handling sequence and rely on `captureReality == .error` plus the generation mechanism to suppress duplicates), or let recovery-attempt failures persist at least an `.error` row and restore `.error` state.

RE-VERDICT: APPROVE

## Re-review round 4

Delta reviewed: commit `5c07b96` only ("Handle repeated overruns and recovery failures"; 2 files, +135/-6), against findings 15 and 16. Re-verified locally: `ShadowCapturePipelineTests` 16/16 pass (including the two new tests). No contract files touched; no API changes; no other source files in the delta.

Disposition of round-3 findings:

15. **FIXED.** `Sources/PrivyCore/Recording/ShadowCapturePipeline.swift:602-615`: a second `.queueOverrun` with the same baseline (`captureEpoch` + `streamEndBeforeDrop`, i.e. no block processed in between) now accumulates `expectedResumedStreamStart += normalizedDrop` instead of overwriting. Verified the invariant that makes the condition sound: the flag is always consumed by `reconciledContinuityGap` before any receive can advance `lastStreamEnd`/the in-flight baseline, so an existing flag with a *different* baseline is unreachable (the replace branch is harmless defensive code). `consecutiveOverrunsAccumulateBeforeTheNextBlock` proves it: two 0.1 s overruns then a block resuming at 4800 yields exactly the two event `.gap` rows summing to 0.2 s, no third remainder row, two `.vadGap` rows.

16. **FIXED.** `ShadowCapturePipeline.swift:907-930`: the latch now scopes only the execution of one `enterTerminalCaptureError` body (`defer { terminalFailureInProgress = false }` at line 930), so a failure during the recovery window is no longer swallowed — post-episode, the latch is false and the full main body runs (stop engine, writer close, persisted `.error`). Mid-flight re-entry is also handled: while a body is executing, a second failure that observes `.error` is correctly dropped as a duplicate, while one that observes `.recovering` (a recovery attempt interleaved into the body's awaits) runs a reduced-but-visible terminal sequence (generation bump, watchdog disarm, `.error` snapshot, fatal capture stop, persisted `.error` row). `failedRecoveryAttemptWhileTerminalEpisodeIsLatchedReturnsToError` proves the user-visible outcome: failed rebuild after a terminal episode ends in `.error` with a persisted "capture restart failed" row and a second fatal stop — never indefinite silent `.recovering`.

Residual findings (corners within corners; recorded per the hard cap, no iteration requested):

17. **MINOR** — `ShadowCapturePipeline.swift:911-926`: the reduced re-entry branch discards `gapStarted`/`gapMessage` — a `heartbeatFault`/`wakeGraceExpired` that interleaves into another episode's mid-flight awaits persists only the `.error` row, not the measured `.gap` the plan pairs with it. Reachable only when a watchdog deadline fires inside another terminal body during `.recovering`.
18. **MINOR** — `ShadowCapturePipeline.swift:911-926`: the reduced branch never closes the writer. A chunk opened during the recovery window by a completed receive whose stale heartbeat kept the state `.recovering` (rather than transitioning to `.recording`) would be left open if this branch then runs; in-flight appends are covered (generation bump + `intentionalStopReason = .fatalError` routes them through `closeWriterAfterStaleAppend`), but an already-open chunk is not, leaving a `recording` row until shutdown/next-launch reconciliation.
19. **MINOR** — test-honesty: `failedRecoveryAttemptWhileTerminalEpisodeIsLatchedReturnsToError` does not actually exercise the reduced branch it is named for — by the time the recovery restart fails, the first body has completed and the `defer` has cleared the latch, so the main body handles it (verified by tracing: the `.recovery` wait ensures the first episode finished). The reduced branch at lines 907-927 is currently untested; its correctness rests on code reading only.

Honest limitations of this round: per instructions I did not re-run the full 59-test suite or the real-model smoke test (only the pipeline suite, 16/16); the multi-failure interleavings in findings 17-18 were analyzed by execution-order reading of the actor-reentrant code, not by reproduction; and the defer-based latch change makes `pause()`/`resume()` usable while in `.error` (guards at lines 209/238 reference only the now-transient latch) — I judge this a benign, arguably useful manual-recovery path, not a defect, but it is a behavior change from round 3 worth noting.

RE-VERDICT: APPROVE

## Re-review round 5

Delta reviewed: commit `da21e86` only ("Wait for VAD snapshot updates in tests"; test-only, +10/-2 in `Tests/PrivyTests/ShadowCapturePipelineTests.swift`, touching the barriers at ~line 794 in `failingVADReportsErrorWhileWriterAndTruthfulSnapshotStayRecording` and ~line 836 in `VADEventStoreFailureKeepsCaptureRecordingAndMarksOnlyVADFailed`). Re-verified locally: `ShadowCapturePipelineTests` 16/16 pass.

Attack results:

- **No test weakening.** Both barriers are strict strengthenings: the old condition (persisted `.vadError` health in the fake store) is kept and conjoined with two new requirements — latest snapshot `capture == .recording` and `vad == .failed`. Every post-barrier assertion (chunk still `recording`, capture `.recording`, vad `.failed`, no fatal stop) is unchanged. Any run that passes the new barrier would have passed the old one; the converse is false.
- **No false positives.** After each barrier, the tests re-read `snapshots.latest()` and assert on it. Between barrier and assertion no new input is injected and the failure conditions are latched (model permanently throwing; `vadPersistenceFailed` latched), so the observed state cannot flip in that window — barrier state and asserted state are the same state.
- **No concealed product bug.** The implementer's race diagnosis matches the code: `consumeVADLane`'s catch persists health via `persistHealth` (visible in the fake store synchronously at the await point) and only then `publishSnapshot()` yields into `bufferingNewest(16)`, whose delivery to the `SnapshotRecorder` consumer task is asynchronous — so a health-only barrier could observe `snapshots.latest()` one snapshot early (`vad == .ready` still) and flake. If the product ever failed to publish the `vad == .failed`/still-`.recording` snapshot, the new barrier times out and fails the test — it surfaces that bug rather than hiding it. The barrier also now enforces the exact user-visible acceptance combination (VAD coverage lost while capture keeps recording) at a single observed instant, which is marginally stronger evidence than the previous two-step check.
- Regression scope: none — no production code in the delta; the other 14 tests in the suite are untouched and still pass.

Findings: none (no BLOCKER/MAJOR/MINOR).

Honest limitations: I ran only the pipeline suite (16/16), per the no-heavy-builds instruction; I did not re-run the writer/VAD suites or the full suite, though this delta cannot affect them (test-only change inside two test methods).

RE-VERDICT: APPROVE
