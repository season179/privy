VERDICT: REJECT

1. **MAJOR — `Sources/PrivyCore/Store/PrivyStore.swift:75-88,91-123,126-161,389-409,526-540` — every guarded state UPDATE ignores the changed-row count.** The SQL includes `WHERE state = ...`, but the implementation never checks `db.changesCount`; reconciliation then inserts a success health row even when a concurrent/external transition made the UPDATE affect zero rows. This directly violates W2 hard constraint (b), which explicitly says a plain guarded UPDATE that ignores the returned changed-row count is a violation. **Fix:** after each guarded UPDATE, require exactly one changed row before returning/logging success; handle zero as an idempotent already-terminal case only after fetching and verifying that exact terminal state, otherwise throw a state-conflict error. Add tests for zero-row checkpoint/finalize/fail/reconcile updates.

2. **MAJOR — `Sources/PrivyCore/Store/PrivyStore.swift:241-264` — reconciliation silently swallows the failure that is supposed to surface an action failure.** The action catch writes its required `.error` health row with `try?`; if the original failure is DB-related, the health insert is likely to fail too, yet `reconcile` returns a projected success report with no observable failure. This violates the requirement that per-action failures be surfaced, never silently swallowed, while still processing the remaining actions. **Fix:** continue applying all actions while collecting failures, persist each health row without `try?`, and after the pass throw an aggregate error if any failure could not be durably surfaced. Add a trigger/injected DB-failure test proving a failed health insert cannot disappear behind a successful return.

3. **MAJOR — `Sources/PrivyCore/Store/PrivyStore.swift:303-326,387-409,559-619` — measurement I/O failures are converted into valid-looking zero/empty metadata and can be committed as `ready`.** `fileSize` maps errors to `0`; `sha256` maps open failures to `nil` and read errors to EOF (potentially hashing only a prefix); `measuredValues` maps a missing inventory/probe/checksum to `(0, 0, "")`; `applyFinalize` never rejects those sentinels. Thus a file that ffprobe decoded but could not be fully hashed, or an inventory that changed between plan/apply, can become `ready` with guessed metadata, violating the absolute “real filesystem size + decoded duration, never stale/guessed metadata” rule. **Fix:** make enumeration, size, and hashing throw; treat any missing file/probe/checksum or read error as a failed measurement; revalidate the measured record before the guarded UPDATE; preserve the file and transition/log `failed` rather than supplying defaults. Add real fixtures for hash/read failure and inventory disappearance.

4. **MINOR — `Sources/PrivyCore/Store/PrivyStore.swift:688-743` — path containment does not stop symlink escapes.** `standardizedFileURL` removes `.`/`..` components but does not resolve filesystem symlinks, despite the comment claiming it does. A row or orphan under `Audio/link/...`, where `link` points outside `audioDirectory`, passes the string-prefix check and lets probing/hashing/file moves operate outside the frozen storage root. This violates the literal path-traversal hard constraint. **Fix:** canonicalize the audio root and every existing path component with symlink resolution (and reject symlink traversal for destinations/nonexistent leaves), then perform containment against the canonical root; add file- and directory-symlink escape tests that verify the outside target is untouched and the row cannot become `ready`.

5. **MAJOR — `Sources/PrivyCore/Store/PrivyStore.swift:246-264,369-385` and `Tests/PrivyTests/ReconciliationTests.swift:360-393` — a recoverable per-file failure leaves an abandoned row in `recording`, and the test calls that success.** When the `.partial` rename fails, the catch logs an error but performs no terminal transition; the test only checks a sibling and health, never checks the failed action's row or reruns after restoring permissions. This violates “no row remains recording unless a live writer owns it” and does not prove the required re-runnable mid-pass state. **Fix:** when the DB remains available, terminally mark the affected row `failed` in the failure transaction while preserving the file; if terminalization itself fails, surface that aggregate failure after completing the pass. Extend the test to assert the failed row is not `recording`, restore the fault, rerun, and verify stable idempotent state; add a separate mid-pass DB-failure case rather than testing only a file rename.

6. **MINOR — `Sources/PrivyCore/Store/PrivyStore.swift:625-683` and `Tests/PrivyTests/ReconciliationTests.swift:128-253` — ffprobe-unavailable behavior is asserted in comments but never exercised.** Every end-to-end probe test runs the host's real ffprobe; garbage covers a nonzero/undecodable result, not missing executable or launch failure. The locator also falls through an invalid `PRIVY_FFPROBE_PATH` to common paths, making unavailability impossible to force on this machine. **Fix:** inject an internal audio-probe/ffprobe locator seam and add end-to-end tests for unavailable executable, launch failure, nonzero exit, timeout, and malformed duration, each proving the file is preserved and the row becomes `failed` without checkpoint metadata.

7. **MINOR — `Sources/PrivyCore/Store/PrivyStore.swift:581-605` — inventory enumeration can silently omit audio files.** A nil enumerator becomes an empty inventory, hidden files are skipped unconditionally, and per-file resource-value errors are discarded with `try?`; reconciliation can then mark owning rows missing or leave orphan audio without any row/health evidence. This conflicts with “every preserved audio file has a row” and the no-silent-failure rule. **Fix:** enumerate all regular audio files without `.skipsHiddenFiles`, install an error handler, and propagate/record enumeration and metadata errors instead of treating them as absence; add unreadable-directory/file fixtures.

8. **MAJOR — `Sources/PrivyCore/Store/PrivyStore.swift:625-652` — each ffprobe timeout busy-waits inside the Store actor for up to ten seconds.** The `Thread.sleep` polling loop never yields actor isolation; multiple hung/corrupt files serialize into `N × 10s` of blocked startup reconciliation, delaying capture and preventing all Store work on that actor. The timeout test is also absent. **Fix:** move process waiting off the actor and await an asynchronous termination/timeout result, terminate and reap the child deterministically, and test many hung probes to prove bounded total progress without blocking unrelated actor calls.

## Re-review

Reviewed `881e912..4b42db2` and reran `swift build --target PrivyCore`, the 26 store/probe tests, 22 reconciliation tests, the full 77-test suite, and Thread Sanitizer for the store/probe/reconciliation suites; all passed. `AudioProbe.swift` is Store-local and every declaration is internal, so it does not widen a frozen/public API or overlap another implementation lane.

Resolution audit:

1. **UNRESOLVED — see finding 1.** Public CRUD checks `changesCount`, but reconciliation still contains unchecked or incorrectly classified zero-row updates.
2. **RESOLVED.** Reconciliation no longer uses `try?` for required health persistence; it continues the pass, collects failures whose terminalization/health transaction also fails, and throws an aggregate.
3. **RESOLVED.** File size and SHA-256 measurement now throw; a missing/failed measurement cannot supply sentinel metadata to a `ready` update, and apply-time size/hash are revalidated.
4. **RESOLVED.** Inventory and action paths use symlink-resolving canonical containment, with an outside-target preservation test.
5. **RESOLVED.** A per-file apply failure now terminalizes the affected row and writes health transactionally; the file-failure test asserts no abandoned `recording` row and stable rerun, and a separate DB-failure test proves aggregate surfacing and convergence.
6. **RESOLVED.** The internal probe/locator seam strictly rejects an invalid configured executable and covers unavailable, launch-failure, nonzero, malformed-duration, timeout, and real-decode classifications.
7. **RESOLVED.** Enumeration no longer skips hidden files or discards resource/enumerator errors; failures propagate and are tested.
8. **UNRESOLVED — see findings 2–3.** Waiting now yields the actor, but the child-termination bound is false and the timeout arbitration has a race. Probe inventory also remains serial, so startup can still cost `N × timeout`.

Findings:

1. **MAJOR — `Sources/PrivyCore/Store/PrivyStore.swift:299-309,473-503,609-619` — finding 1's changed-row-count ruling is still not implemented for reconciliation.** `surfaceReconciliationFailure` explicitly ignores `changesCount`; `markFailedAndLog` never reads it; and `applyFinalize` turns every zero-row result—including a missing row or an unexpected/future state—into a warning health row and successful return instead of accepting only the exact intended terminal state. The new guard tests at `Tests/PrivyTests/StoreTests.swift:323-391` exercise only public CRUD, so these paths remain uncovered. **Fix:** centralize guarded-update classification: require one changed row, otherwise fetch the row and accept only the operation's intended terminal state (`ready` for finalize, `failed` for terminalization); throw/aggregate missing or conflicting states. Add reconciliation tests that force each zero-row path.

2. **MAJOR — `Sources/PrivyCore/Store/AudioProbe.swift:143-157` — the claimed ffprobe timeout is not actually bounded.** After the grace period the code sends SIGTERM and SIGINT only, then calls blocking `process.waitUntilExit()` with no further deadline. A child that ignores both signals hangs reconciliation forever. This was reproduced against the production `AudioProbe.swift`: a 100 ms `BoundedProcess.run` using a script that traps TERM/INT exceeded a 5 s external timeout and left child processes. The tests use `/bin/sleep`/a normal shell, which cooperate with SIGTERM, so they cannot catch this. **Fix:** escalate to SIGKILL after the grace period, reap through asynchronous termination rather than an unbounded wait, and test a signal-ignoring executable plus absence of lingering parent/child processes. Use bounded concurrency for inventory probes (or otherwise cap total startup time), because the sequential loop at `PrivyStore.swift:361-389` still makes many timeouts additive.

3. **MINOR — `Sources/PrivyCore/Store/AudioProbe.swift:132-155` — timeout and natural-exit arbitration can misclassify a successful probe as timed out.** The timeout task checks `process.isRunning` and only afterward marks `timedOut`; a natural exit in that window can have its handler suppressed by the newly-set timeout flag, after which the timeout task wins the continuation. This fails safe (the file is preserved/failed), but records a false probe failure. **Fix:** atomically claim the one-shot outcome and timeout flag under the same lock, with exactly one path responsible for classifying and resuming.

RE-VERDICT: REJECT

## Re-review round 2

Reviewed the inclusive round-2 delta (`94625c6`, `20e6a23`, `a79e155`). Targeted `AudioProbeTests` (9 tests) and the four changed reconciliation tests passed.

Resolution audit:

1. **RESOLVED.** Guarded reconciliation writes now route through `classifyGuardedUpdate`; one-row, intended-terminal, conflict, and missing outcomes are classified. Missing/conflicting apply outcomes throw into reconciliation's durable failure-surfacing path rather than being logged as successful `ready` transitions.
2. **PARTIALLY RESOLVED.** TERM/INT-ignoring direct children are escalated to SIGKILL and reaped, but inventory has no total deadline and naturally completed probes retain timeout resources for the full ten seconds.
3. **RESOLVED.** Natural exit and timeout now contend for one lock-protected `ProbeState.claim()`; the separate racy timeout flag is gone.

Findings:

1. **MAJOR — `Sources/PrivyCore/Store/PrivyStore.swift:965-990` and `Tests/PrivyTests/ReconciliationTests.swift:626-654` — four-wide batching is not the required total startup cap.** `probeAudioFilesBounded` waits for successive batches with no whole-inventory deadline, so worst-case startup remains `ceil(N/4) × (10 s timeout + 300 ms signal grace)`. Forty hung recovered files can still delay capture by roughly 103 seconds. The test does not prove the fix: its 20 probes each sleep 40 ms and its limit is 6 seconds, while even the old serial implementation takes only about 0.8 seconds. **Fix:** impose a fixed deadline over the entire probe phase, cancel/terminate in-flight probes at that deadline, classify unfinished files as failed while preserving them, and test enough cancellation-aware hung probes to distinguish concurrent execution and prove the total cap.

2. **MAJOR — `Sources/PrivyCore/Store/AudioProbe.swift:133-164` — every naturally completed probe retains two pipe descriptors until its detached timeout sleep expires.** The natural-exit handler wins `claim()` and resumes, but the fire-and-forget timeout task is neither canceled nor released; it holds the `Process` and both `Pipe`s for the default ten-second timeout. Running the production `AudioProbe.swift` against 300 sequential `/usr/bin/true` processes increased the harness's open descriptors from 4 to 604—exactly two per completed probe. This defeats the resource bound and can turn valid recovered files into false launch failures once the process hits its descriptor limit. **Fix:** use a cancellable structured race, cancel and await the timeout task when natural exit wins, clear the termination handler, and close pipe handles deterministically. Add a regression test that runs hundreds of fast successful processes and verifies descriptor usage returns near baseline immediately rather than after ten seconds.

RE-VERDICT: REJECT

## Re-review round 3

Reviewed delta `a79e155..853d0b4` (single commit `853d0b4`, 4 files, all inside `Sources/PrivyCore/Store/` + `Tests/PrivyTests/`). Ran `AudioProbeTests` (11 tests), the full `ReconciliationTests` suite (25 tests), plus 3 repeat runs of the FD and cancellation tests with a post-run `pgrep -f privy-trap` after each — all green, zero orphans. Tree is clean; no `public` symbols added (`ProcessExecution`, `ProbeInventoryEvent`, `DefaultInventoryProbeDeadline` are private; the new `inventoryProbeDeadline` init parameter is internal with a default).

Resolution audit of the two remaining MAJORs:

1. **RESOLVED — whole-inventory fixed deadline.** `probeAudioFilesBounded` (`Sources/PrivyCore/Store/PrivyStore.swift:985-1054`) replaces batch-waiting with dynamic four-wide scheduling under one `Task.sleep(for: deadline)` arm (default 11 s, line 9 — deliberately > 10 s per-probe timeout + 0.3 s signal grace, so small inventories classify by per-process result). Every file is pre-classified `.failed("inventory probe deadline exceeded")` (lines 1001-1005) and only results completed before the deadline overwrite that, so unfinished files are preserved as failed measurements and can never become `ready` with guessed metadata. At the deadline `group.cancelAll()` cancels in-flight probes; `BoundedProcess.run`'s `withTaskCancellationHandler` (`AudioProbe.swift:121-129`) turns that into TERM→INT→SIGKILL→`waitUntilExit()` reap of the real ffprobe child (`AudioProbe.swift:303-317`). Startup cost is now `deadline + ~0.3 s` regardless of N.
2. **RESOLVED — natural exit releases timeout task and pipe descriptors immediately.** The natural-exit path claims the one-shot outcome, cancels **and awaits** the timeout arm, drains stdout, then closes both pipe handles and clears the termination handler before resuming (`AudioProbe.swift:247-272`, `cleanupHandles` at 319-326). The 300-fast-process test passes in ~0.32 s against a `/dev/fd` count with a +24 tolerance; the old code would have accumulated ~600 descriptors (2 per completed probe, all still inside the 10 s detached-sleep window) and tripped the assertion, so the test genuinely detects the old leak.

Continuation-ownership audit (cancellation/timeout/natural exit/launch failure):

- Exactly one owner: `claimLocked` (`AudioProbe.swift:288-296`) nils the continuation under the lock and hands a single `Ownership` to the winner; natural exit, timeout, cancellation, no-launch cancellation, and launch failure all route through it, so double-resume is structurally impossible and every loser defers.
- No deadlock: the natural-exit winner only awaits the cancelled timeout arm, which is always a cancellable `Task.sleep`; the timeout arm never waits on the natural-exit path. Cancel-before-launch (`cancellationWasRequested` → `finishWithoutLaunchAsTimeout`), cancel-during-launch (`markLaunchFinished` → `cancel()`), and cancel-after-exit (claim fails, no-op) are each covered, including the `armTimeout` storage race (task cancelled if the claim already left `.pending`).
- Orphan/race context: the signal-ignoring fixture (`Tests/PrivyTests/StoreTests.swift:600-607`) busy-loops with shell builtins only (`while :; do :; done`) — no grandchild exists to orphan, and SIGKILL + `waitUntilExit()` reaps the single child. My 3 post-run pgrep checks found zero orphans, consistent with the orchestrator's 25× result; I found no code evidence of a late/orphan race contradicting it. The cancellation test would also catch the old code, which had no cancellation wiring at all (it would sit for the full 10 s timeout and fail the 2 s elapsed bound).
- Test honesty: the inventory test (`ReconciliationTests.swift:626-655`) distinguishes a fixed total cap from batching — a per-batch deadline would take ~0.4 s for 8 files vs ~2 s for 40 files and fail the `|elapsed diff| < 0.5` assertion, and `started == 4` with 40 files proves no later batch ever launches. `cancelled == 4`, zero `recording` rows, and all files preserved are asserted for both sizes.

Findings:

1. **MINOR — `Sources/PrivyCore/Store/PrivyStore.swift:1038-1047` — a probe completing after the deadline arm fires but before the `.deadline` event is dequeued starts one extra probe.** Events are consumed one at a time; a `.result` processed in that window calls `addProbe`, and the new probe is immediately cancelled when the `.deadline` event is consumed next. The total-time cap still holds (the extra probe is cancelled within microseconds and `BoundedProcess` termination is bounded), so this is cosmetic drift from "at most 4 ever start", not a deadline violation.
2. **MINOR — `Sources/PrivyCore/Store/AudioProbe.swift:303-317` — `terminateReapAndFinish` blocks a cooperative-pool thread in `waitUntilExit()` for up to ~300 ms + reap.** Bounded by the untrappable SIGKILL and at most four concurrent at the inventory deadline, so it cannot stall the Store actor or the probe group; noted only because it is the one remaining blocking wait on the cancellation/timeout path.

RE-VERDICT: APPROVE
