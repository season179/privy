VERDICT: REJECT

1. **BLOCKER — `Sources/PrivyCore/Monitoring/AudioDeviceMonitor.swift:113-118, 168-200` — partial listener registration leaks an untracked `passUnretained` callback.** `start()` sets `started = true` and starts the forwarder before registration succeeds; `installCoreAudioListeners()` attempts both registrations, checks both afterward, and records neither address unless both succeed. If either call succeeds and the other fails, the successful listener remains installed, `registeredAddresses` is empty, retry is blocked, and CoreAudio can later dereference a deallocated `rawEmitter`. **Fix:** register/check listeners sequentially, append each successful registration immediately, roll back every installed listener on any error, cancel the forwarder, and set `started = true` only after the whole installation commits; add injected registrar tests for first-listener failure and second-listener failure.

2. **MINOR — `Sources/PrivyCore/Monitoring/AudioDeviceMonitor.swift:33-42, 70-78` — a mixed 500 ms burst is coalesced in time but still exported as two independently handled events.** A normal AirPods transition raises both properties; the monitor publishes `.defaultInputChanged` and `.deviceListChanged` back-to-back, but the frozen `SystemMonitoring` stream has no batch marker and W5 forwards events sequentially, so W4 cannot know they represent one restart sequence without adding its own undocumented debounce. **Fix:** emit exactly one event per window, preferring `.defaultInputChanged(latestUID)` when present and otherwise `.deviceListChanged`, or explicitly assign and test a second coalescing layer in W4 before workers rely on this behavior.

3. **MAJOR — `Sources/PrivyCore/Monitoring/SleepWakeMonitor.swift:25-55` and `Tests/PrivyTests/ClockAndMonitorTests.swift:232-278` — the only NSWorkspace callback path has zero monitor-level tests and omits the literal nonisolated-factory requirement.** The handlers are explicitly `@Sendable` and capture only the emitter, which is good, but they are declared inside actor-isolated `start()` rather than returned by the frozen spec's `nonisolated` factory. None of the 20 tests instantiates `SleepWakeMonitor`, registers/removes observers, or invokes these handlers; direct `MonitorEventStream` emissions do not prove this boundary. **Fix:** move handler creation into a `private nonisolated static` factory and inject a closure-based notification source/registrar so tests verify start/stop registration, will-sleep/did-wake order and timestamps, observer removal, idempotence, and post-stop silence without AppKit UI.

4. **MAJOR — `Tests/PrivyTests/ClockAndMonitorTests.swift:286-363` — the AudioDeviceMonitor tests exercise the debouncer seam, not the monitor lifecycle claimed by “20/20 pass.”** Every device test calls `inject*` directly without `start()`, bypassing the raw emitter, forwarder, CoreAudio UID enrichment, listener registration/removal, registration errors, and `started` state. That is why finding 1 passes unnoticed. **Fix:** inject a CoreAudio registration/query adapter, start the real monitor in tests, drive captured C-callback equivalents through the raw path, and assert registration rollback, forwarder delivery, UID enrichment, stop removal, and no event after stop; retain focused debouncer unit tests separately.

5. **MINOR — `Sources/PrivyCore/Monitoring/MonitorEventStream.swift:21-22, 29-50` — monitor events can be silently discarded despite W1's explicit no-silent-discard constraint.** `.bufferingNewest(256)` evicts events when full and `emit()` ignores `Continuation.YieldResult`, so neither tests nor health can know a sleep/device event vanished. **Fix:** use an unbounded stream for this low-volume monitor contract, or inspect `.dropped` and expose a non-lossy overflow signal; add a saturation test proving the chosen policy.

6. **MINOR — `Sources/PrivyCore/Contracts/AudioContracts.swift:42-54,77-80`, `ClockContracts.swift:24-28`, `HealthContracts.swift:54-74,85-88,99-104`, `PipelineContracts.swift:43-48,79-93`, and `StoreContracts.swift:14-18,33-38,47-52,72-77,91-111,121-126,134-138` — the frozen contract is widened with public initializers absent from the plan.** The commit rationale says W2-W4 and tests are separate modules, but W2-W4 compile inside `PrivyCore` and tests already use `@testable`; these initializers are therefore unnecessary exported API. **Fix:** make these memberwise initializers internal (or remove the explicit declarations where synthesis suffices) and leave only the properties/cases/protocol requirements frozen in `plan.md`.

7. **MINOR — `Sources/PrivyCore/Foundation/AppPaths.swift:10-16` — production storage silently falls back to a purgeable temporary directory.** W1 freezes the live root at `~/Library/Application Support/Privy`; if lookup unexpectedly fails, recording to `$TMPDIR/Privy` makes audio appear durable while macOS may remove it. **Fix:** fail startup visibly when Application Support cannot be resolved instead of selecting a temporary root; keep temporary roots available only through the explicit test `layout(rootedAt:)` path.

8. **MINOR — `Tests/PrivyTests/PrivyTests.swift:1-7` and `Sources/PrivyCore/PrivyCore.swift:1-3` — W1 deletes files outside its exclusive ownership.** The implementation plan assigns removal of the placeholder test to W5, and neither deleted path is in W1's owned-file list; this creates avoidable cross-lane conflict. **Fix:** restore both placeholders on this branch and let the designated integration/orchestrator lane remove them.

## Re-review
RE-VERDICT: APPROVE

1. **RESOLVED:** listener registration is sequential and commit-after-success, partial installs are rolled back, and injected first/second-registration failure tests cover both branches.
2. **RESOLVED:** each 500 ms mixed burst now emits exactly one event, preferring the latest default-input UID, with focused tests for both event choices.
3. **RESOLVED:** sleep/wake handlers come from a static nonisolated factory and fake-registrar tests exercise real start, ordered callbacks, removal, idempotent start, and post-stop silence.
4. **RESOLVED:** injected CoreAudio adapter tests now start the real monitor and drive captured C callbacks through raw emission, UID enrichment, debouncing, listener removal, and rollback paths.
5. **RESOLVED:** monitor streams default to unbounded delivery and a 1,000-event saturation test verifies no silent loss.
6. **RESOLVED:** all extra public contract initializers were removed, restoring the frozen declaration surface.
7. **RESOLVED:** production Application Support lookup now throws visibly instead of falling back to temporary storage.
8. **RESOLVED:** both orchestrator/W5-owned placeholder files were restored.

**New findings:** none. `swift build --target PrivyCore`, all 28 `ClockAndMonitorTests`, and the same suite under Thread Sanitizer pass at `297f7a6`.
